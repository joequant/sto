version:  '3'
services:
  core-dev:
     image: "joequant/core-dev"
     volumes:
       - user:/home/user
       - log:/var/log
     environment:
       - INSTANCE_NAME=core-dev

volumes:
      user:
      log:
